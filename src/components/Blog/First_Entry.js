import NavBar from '../Nav-Bar';
import Header from '../Header';

const FirstEntry = () => {
    return (
        <body>
            <Header></Header>
            <NavBar></NavBar>
        </body>
    );
}

export default FirstEntry